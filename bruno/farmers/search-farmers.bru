meta {
  name: Search Farmers
  description: Quick search farmers for autocomplete (by name, phone, or code)
}

auth: bearer

method GET
url {{base_url}}/farmers/search?q=kumar&limit=10

headers {
  Accept: application/json
  Authorization: Bearer {{access_token}}
}

script:post-response {
  const response = res.getBody();
  bru.assertion(expect(res.getStatus()).to.equal(200));
  bru.assertion(expect(response).to.have.property('success'));
  bru.assertion(expect(response.success).to.equal(true));
}
